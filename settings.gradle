pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // 改为允许柔性读取配置
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "GameShell"
include ':app'
